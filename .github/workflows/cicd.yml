name: Todolist CI/CD

on:
  push:
    branches:
      - master
    paths-ignore:
      - "README.md"
  pull_request:
    branches:
      - master

jobs:
  continuous-integration:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Backend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/finalproject-backend:latest ./server
          docker push ${{ secrets.DOCKER_USERNAME }}/finalproject-backend:latest

      - name: Build and Push Frontend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/finalproject-frontend:latest ./client
          docker push ${{ secrets.DOCKER_USERNAME }}/finalproject-frontend:latest

  continuous-deployment:
    needs: continuous-integration
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Docker Rootless Environment
        run: |
          echo "Setting up Docker rootless env..."
          echo "export PATH=$HOME/bin:$PATH" >> $GITHUB_ENV
          echo "export DOCKER_HOST=unix://$XDG_RUNTIME_DIR/docker.sock" >> $GITHUB_ENV

      - name: Pull Latest Backend Image
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/finalproject-backend:latest

      - name: Pull Latest Frontend Image
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/finalproject-frontend:latest

      - name: Stop and Remove Old Containers
        run: |
          docker compose down || true
          docker system prune -f

      - name: Run New Containers
        run: |
          docker compose up -d
        env:
          PORT: 3018
          CONNECTION_URL: mongodb://e2425-wads-l4bcg2:motis6u2@localhost:27017/e2425-wads-l4bcg2?authSource=e2425-wads-l4bcg2
          EMAIL_USER: cottoneightservice@gmail.com
          EMAIL_PASSWORD: dcoe wiwm hrbz wwwe
          CLOUDINARY_CLOUD_NAME: dfe7uyntq
          CLOUDINARY_API_KEY: 892865934638977
          CLOUDINARY_API_SECRET: QoHBalqcQPTHWBJ1VQ88kv4_eOw
          CLOUDINARY_URL: cloudinary://892865934638977:QoHBalqcQPTHWBJ1VQ88kv4_eOw@dfe7uyntq
          EMAIL_SERVICE: gmail
          GOOGLE_CLIENT_ID: 310044493415-q6tk0ko1vemi8js1ht290pop4ircqgfg.apps.googleusercontent.com
          GOOGLE_CLIENT_SECRET: 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
          FRONTEND_URL: https://e2425-wads-l4bcg2-client.csbihub.id